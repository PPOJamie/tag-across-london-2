# Tag Across London

Upload the files in this folder to the root of a GitHub repository, then enable GitHub Pages from the `main` branch and `/ (root)` folder.

Open `index.html` locally or deploy the repository as a static site.
